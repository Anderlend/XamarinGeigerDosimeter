<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinGeigerDosimeter.Views.LoginPage">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <Grid VerticalOptions="CenterAndExpand">
                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <Entry Placeholder="UserName" x:Name="EntryUser" Grid.Row="0"></Entry>
                    <Entry Placeholder="Password" IsPassword="True" x:Name="EntryPassword" Grid.Row="1"></Entry>
                    <Button Text="LogIn" Grid.Row="2" BackgroundColor="#0077be" TextColor="White" Clicked="Button_Clicked_1"></Button>
                    <Button Text="SingUp" Grid.Row="3" BackgroundColor="#0077be" TextColor="White" Clicked="Button_Clicked"></Button>
                </Grid>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>